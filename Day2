#include <stdio.h>

int main() {
    int n, pos;

    // Read size of array
    scanf("%d", &n);

    int arr[1000];  // buffer large enough for typical constraints

    // Read array elements
    for(int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    // Read position to delete (1-based)
    scanf("%d", &pos);

    // Convert to 0-based index
    int index = pos - 1;

    // Shift elements to the left
    for(int i = index; i < n - 1; i++) {
        arr[i] = arr[i + 1];
    }

    // Print updated array
    for(int i = 0; i < n - 1; i++) {
        printf("%d", arr[i]);
        if(i < n - 2) printf(" ");
    }

    return 0;
}
